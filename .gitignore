# Dependencies
node_modules
.pnp
.pnp.js

# Build outputs
dist
build
*.tsbuildinfo

# Environment variables
.env
.env*.local
.dev.vars

# IDEs and editors
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea
*.swp
*.swo
*~
.DS_Store

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Turbo
.turbo
.nx

# Better-T-Stack
.alchemy

# Testing
coverage
.nyc_output

# Misc
*.tgz
.cache
tmp
temp

# tsx runtime temp dirs (e.g. tsx-501)
tsx-*/

# Local datasets (large)
apps/web/data-resources/

# Wrangler dev state outside apps/web (which has its own .gitignore)
.wrangler/
.gstack/

# OSRM prebuilt routing graph (multi-GB; built locally via infra/osrm/build-graph.sh)
infra/osrm/data/

# Design-handoff archives (kept on disk, not committed to git history)
*.zip

# --- Machine, tool, and build junk (stop working-tree noise) ---
# FUSE filesystem temp files (artifacts from deletes on a mounted fs)
**/.fuse_hidden*
**/.thumbnail
# pnpm content-addressable store
.pnpm-store/
# wrangler update-check cache
update-check/
# gstack / superpowers session working dir
.superpowers/
# SSR build cache (random-id dir, previously committed by accident)
Rg4xacSoJdQxVWTQ6Npgo/
# Local brand/planning artifacts kept on disk, not tracked
/*.pdf
/*.docx
/Locnative.com footer requirement/
